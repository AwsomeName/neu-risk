# 项目 Agent 约定

更新日期：2026-09-10。适用于本项目内的研究、文档和后续实验。

## 已确认的术语

- 本项目中的“现代大模型”专指 **Transformer 系列模型**。这是用户约定的研究范围，不是对整个领域的通用定义。
- 讨论 CNN、RNN、状态空间模型、脉冲神经网络（SNN）等时，直接写明架构名称，不默认将其纳入上述术语。
- 对含 Transformer 的混合系统，说明保留、修改或替换了哪些模块；不能仅因某个组件不是 Transformer 就把整个系统归为非 Transformer。
- VLA（视觉—语言—动作）描述模型的输入输出与任务组织，不限定唯一架构；逐个核对视觉编码器、骨干、动作模块和控制系统。

## 用户当前的研究问题

用户希望降低 Transformer 的运行功耗与任务能耗，特别关注机器人端侧任务中的能力、实时性与能源约束。原有 Hebbian 调研作为研究材料，不预设 Hebbian 是解决能耗问题的必选机制。

2026-09-10 用户已确认当前主线：并行研究整个 Transformer 架构与生物神经系统，围绕共同计算问题寻找差异和可借鉴机制，通过架构改进降低运行能耗，同时保持任务能力与实时性。原先三条路线保留为讨论背景；直接工程优化作为重要对照，不再将当前方向描述为尚未收敛的三选一。

- 应用载体是连续视听环境理解：持续视觉输入为主，可结合流式 ASR，保留并更新环境状态、按需响应。应用场景服务于架构节能主线。
- 连续处理应满足明确的因果与缓冲约束，跨时间保留状态、长期资源有界；连续输入不等于持续输出或在线更新权重。对照需包含已有流式 Transformer。
- 不先押注 Hebbian、SNN、稀疏、预测编码等机制，也不在改完架构后再寻找生物解释。候选方案需形成“生物证据 → 计算机制 → 架构改动 → 节能来源与额外开销 → 对照实验”的证据链。
- 第一阶段在与目标大模型结构对应的小规模基线上验证，不能随意用结构不同的小模型替代。记录保留的核心机制与缩减维度；改进版允许明确的实验性结构变化，再以多个规模检验可扩展性。
- 已确认阶段计划：建立两侧全貌 → 确定任务、架构与硬件并建立基线 → 筛选一个假设 → 小规模验证 → 扩大规模、时长和场景验证。详细记录见研究方向讨论备忘第 13 节。

将研究建议与用户已作出的决定分开。具体任务、模型、硬件、功率预算、能力门槛和生物机制尚未确定，不能自行写成已确认的实验约束。可先使用按时间播放的视听数据，后续进入机器人闭环；项目目前尚未启动模型实验。

## 分析与证据要求

- 区分生物结构的一一映射、计算机制的类比和系统组织原则的借鉴。没有一一映射不意味着不能借鉴；功能相似也不证明机制相同。
- 区分训练、推理和在线适应的成本。替换反向传播或引入 Hebbian 更新，不自动降低部署时的推理能耗。
- 区分功率（W）、能量（J/任务）、延迟、吞吐和任务成功率。“能效”必须给出具体口径，不能仅用参数量或 FLOPs 推断实测节能。
- 区分实测板卡/整机功耗、芯片功率配置或 TDP、算子能耗估算。记录内存、数据搬运、调度开销和硬件对稀疏/事件计算的实际支持。
- 机器人评估需分别说明感知、语义推理、动作生成和底层控制的调用频率；动作执行频率不等于整套模型的推理频率。
- 说明本地与远端计算边界，以及传感器、计算、通信、执行器是否计入。不能把服务器代算的演示当作完整端侧部署。
- 人脑约 20 W 仅作为有来源的数量级参照，不写成严格小于 20 W，不视作整个人体能耗，也不能直接据此计算与特定模型的任务能效差距。
- 不预设“Transformer/VLA 无法端侧部署”“SNN 必然省电”或“生物启发必然优于工程优化”。逐项核查模型、任务、硬件、测量条件和对照。
- 对生物启发方案，明确机制、预期减少的计算或数据搬运、额外开销、失效条件，以及同等任务约束下的普通工程基线。
- 没有同口径脑活动数据与映射模型时，不对比大脑和 MoE 的“激活参数量”。突触总数与模型总参数只能作有明确假设的规模粗比，不能换算智能或能效。
- 交流先给直接结论或数字，再简述适用边界；用户明确要求简洁，避免反复解释已经约定的限制。

## 文档状态

先读 README.md 了解文档职责。历史归档仅用于回溯，不作为当前事实依据。区分论文报告、源码检查与本地实验；项目目前尚无模型复现结果。用户的新约定优先于旧文档中的表述。
