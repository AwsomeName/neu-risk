# Hebbian 实验与架构记录规范

修订日期：2026-09-09。定位：复现记录规范；原来重复且未核实的架构结论由[架构主文档](/Users/lc/Desktop/neu-risk/Hebbian学习-网络架构完整调研报告.md)取代。本页是后续执行标准，不表示实验已经完成。

## 1. 一项结果对应一条记录

| 字段 | 必须记录的内容 |
|---|---|
| 研究问题 | 比较学习规则、整套系统，还是某项生物约束？ |
| 文献身份 | 作者、题名、年份、DOI/arXiv、具体版本 |
| 证据位置 | 章节、表格、图或附录，不只放论文首页 |
| 实现身份 | 作者原实现/第三方复现；仓库、commit、运行入口 |
| 结构 | 每层类型、尺寸、激活、归一化、池化、残差与实际执行顺序 |
| 动力学 | 连续或脉冲，状态变量、时间步、复位与时间常数（如适用） |
| 更新 | 方程、mode、学习率、竞争、阈值、归一化和参数冻结 |
| 信息 | 每阶段可访问哪些标签、奖励、活动或反馈 |
| 数据 | 数据集版本、划分、预处理、增强与标签比例 |
| 训练 | 预训练、分类头、微调、外层搜索的全部预算 |
| 评估 | 指标、模型选择、随机种子、重复次数与统计方式 |
| 资源 | 硬件、批量、时间、峰值内存；能耗需声明测量边界 |
| 项目验证 | 仅阅读/静态核对/运行成功/复现结果；保留日志路径 |

## 2. 读代码时的顺序

先找数据入口和变换，再看构造函数、前向执行、权重更新和训练循环。检查：初始化的模块是否真的被调用；传入的参数是否在选中分支生效；特征层是否冻结；分类头是否用标签；使用自动微分的作用是什么。

复现所需依赖以固定版本仓库为准，不根据文中伪代码自行补成“官方实现”。

## 3. 两种比较不能混用

**规则比较**：尽可能固定架构、输入、激活、预处理和预算，只改变明确的学习机制；若必须调整超参数，记录搜索范围和预算。不同规则可能有不同适用条件，应如实报告。

**系统比较**：允许各自采用合适的架构和训练流程，但把全部成本计入，明确资源、数据和部署约束。不将系统差异全部归因于某条更新规则。

## 4. 最小对照建议

是否采用以下对照取决于研究问题：随机特征+相同读出；仅训练读出；端到端 BP；目标方法；移除关键机制的消融。设计应检验具体解释，不为凑齐清单而运行实验。

仅特征训练 epoch 少，不代表全流程快；测试集不同 epoch 均值也不是多随机种子均值。不要在测试集上选择超参数后，再将该测试值视为无偏泛化估计。

## 5. 空记录模板

```text
研究问题：
论文与版本：
证据位置：
仓库与 commit：
数据入口与划分：
实际前向结构：
更新规则与可用信息：
训练阶段与预算：
对照与指标：
模型选择和重复实验：
结果与日志：
限制：
项目验证状态：待执行
```

已知缺口见[待核验清单](/Users/lc/Desktop/neu-risk/Hebbian学习-网络架构补充调研.md)。
