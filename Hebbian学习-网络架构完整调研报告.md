# Hebbian 网络架构：已核对摘录与复现边界

修订日期：2026-09-09。定位：架构主文档。保留原文件名，撤回旧版“完整、100% 确定”的保证。本项目没有实际模型代码或训练结果；以下是文档配置的静态复算与公开源码对照，不是经过运行验证的复现方案。

## 1. 来源及适用范围

主要对照 Nimmo 与 Mondragón 作者仓库的 `Hebbian_Code/models/model_hebb.py`，本次可访问的 main 版本。旧文没有保存当时 commit；本次也尚未建立可运行的固定版本检出，所以所有“对应原论文实验”的判断仍需另行核对。[源码](https://raw.githubusercontent.com/Julian-JN/Advancing-the-Biological-Plausibility-and-Efficacy-of-Hebbian-Convolutional-Neural-Networks/main/Hebbian_Code/models/model_hebb.py)

这里的 `softhebb`、`miconi`、`lagani` 是该仓库中的配置名称。它们不是方法家族定义，也不能代替各自作者的原始实现。构造函数传入了参数，不足以证明对应机制在选定 mode 下生效。

## 2. 32×32 RGB 输入的空间配置摘录

假设普通卷积、stride=1、dilation=1，池化采用下面指定值。操作顺序应从前向方法读取：本次源码中通常为 BN → Conv → Triangle → Pool，不能按初始化语句排列顺序推断。

| 配置 | 通道变化 | 卷积 kernel / padding | 池化 | 各阶段输出 |
|---|---|---|---|---|
| softhebb | 3→96→384→1536 | 5/2，3/1，3/1 | Max(4,2,1)，Max(4,2,1)，Avg(2,2,0) | 16×16×96；8×8×384；4×4×1536 |
| miconi | 3→100→196→400 | 5/0，3/0，3/0 | 每层 Avg(2,2,0) | 14×14×100；6×6×196；2×2×400 |
| lagani_short | 3→96→128→192 | 5/0，3/0，3/0 | Max(2,2,0)，无，Avg(2,2,0) | 14×14×96；12×12×128；5×5×192 |
| lagani | 3→96→128→192→256 | 5/0，3/0，3/0，3/0 | Max(2,2,0)，无，Avg(2,2,0)，无 | 14×14×96；12×12×128；5×5×192；3×3×256 |

池化三元组按 kernel、stride、padding 排列。以上从旧文已有配置复算，和本次可访问模型定义核对；不包含学习 mode、数据变换和完整超参数。

对应 Flatten 维数分别为 24576、1600、4800、2304。当前源码在分类线性层前使用 Dropout。Triangle 的中心化、幂次和实现细节也应记录，不能用普通 ReLU 无条件替代。

## 3. 参数量复算

以下只计算表中普通非分组卷积和分类线性层的权重，不含偏置、额外可训练变量或 buffer。不同实现应按其实际注册参数重新统计。

| 配置 | 卷积权重 | 10 类线性头权重 | 权重合计 |
|---|---:|---:|---:|
| softhebb | 5,647,392 | 245,760 | 5,893,152 |
| miconi | 889,500 | 16,000 | 905,500 |
| lagani_short | 338,976 | 48,000 | 386,976 |
| lagani | 781,344 | 23,040 | 804,384 |

普通卷积权重数为 C_out × C_in × k_h × k_w。以 miconi 配置为例：

```text
3×100×25 + 100×196×9 + 196×400×9 + 1600×10 = 905500
```

旧文的约 0.4M 和“比 SoftHebb 少 15 倍”与该配置不符。padding 本身不改变卷积核权重数，但可能通过输出尺寸影响后面的 Flatten+Linear。

## 4. 肿瘤分类段：旧规格不可执行

旧文假设输入 128×128、六次“3×3 无填充卷积+2×2 池化”。按默认向下取整，尺寸如下：

| 阶段 | 卷积后 | 池化后 |
|---|---:|---:|
| 输入 128，第 1 层 | 126 | 63 |
| 第 2 层 | 61 | 30 |
| 第 3 层 | 28 | 14 |
| 第 4 层 | 12 | 6 |
| 第 5 层 | 4 | 2 |
| 第 6 层 | 输入 2 小于 kernel 3，无法执行 | 不适用 |

因此撤回原“128→63→30→14→7→3→1”和“512→1024”的完整规格。当前可访问源码的首个线性头输入为 2048；实际图像 resize、运行入口和版本仍须核实。这里不为使尺寸对上而猜一个新输入，也不将无效规格列入可复现方案。

## 5. FastHebb 必须单独记录

原文后期把 FastHebb 调研换成 SoftHebb 配置，这不是完成了原来的调查。FastHebb 2022 版第 4.2 节描述受 AlexNet 启发的六层网络及 ImageNet 上的更大变体；还包含预训练后端到端微调。不能套用本页的 softhebb 表格。[FastHebb 原文](https://arxiv.org/html/2207.03172v1)

## 6. 规则与参数语义

`t_invert` 在 SoftHebb 作者实现中缩放 softmax 输入，起逆温度作用，不是时间反转或 STDP 参数。[作者实现](https://raw.githubusercontent.com/NeuromorphicComputing/SoftHebb/main/utils.py)

Hard-WTA 描述竞争选择，不自动意味着奖励调制。BCM 阈值、侧抑制参数也不能直接当成全局第三因子。需要继续读层的更新代码、mode 分支、冻结设置和训练循环。

## 7. 本页不保留哪些比较

不把不同来源准确率挂到这些构造函数上；不报告未核对的“标准 BP >90%”；不把某个 100—256—50 实例当作 STDP 的通用网络。Oja/BCM 也不限定只能使用 1—3 层 MLP。

性能来源集中在[方法与证据](/Users/lc/Desktop/neu-risk/Hebbian学习的现代实现-专题调研.md)。复现前仍需补全[记录规范](/Users/lc/Desktop/neu-risk/Hebbian学习-网络架构详细调研.md)中的版本、数据和运行字段。
